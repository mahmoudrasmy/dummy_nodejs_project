pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'npm --version'
            }
			steps {
                sh 'echo "sucess"'
            }
        }
    }
}